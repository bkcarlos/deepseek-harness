/** Host loader entry for the desktop menu-action browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the menu-action dispatcher. */
export function apply(): void {}
