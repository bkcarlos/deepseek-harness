/** Host loader entry for the update-settings browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the update settings section. */
export function apply(): void {}
